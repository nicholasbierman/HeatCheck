import React from 'react';
import { NavLink } from 'react-router-dom';

export const NavBar = () => {
  return (
    <nav style={ { height: "15px", color: "orange", margin: "0 auto" } }>
      <ul>
      </ul>
    </nav>
  );
}